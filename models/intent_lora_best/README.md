# Intent LoRA Adapter

This directory contains the trained LoRA/PEFT adapter generated during
the fine-tuning pipeline.

The adapter can be loaded with the corresponding base model and used for
intent extraction in the Hybrid RAG workflow.
